---
created: 2026-04-20
source_filename: "roc_curves_binary.png"
file_type: png
tags: [thesis-chart, ML, ROC-curve, AUC, binary-classification, model-comparison, ml_model]
generated_by: ml_model.py
---

# ROC Curves — Binary Classification Model Comparison

## What This Chart Shows

Receiver Operating Characteristic (ROC) curves for all three ML models (Logistic Regression, Random Forest, Gradient Boosting) on the binary classification task (up vs. down returns). Generated by `ml_model.py`.

## Expected Content

- X-axis: False Positive Rate (1 − Specificity)
- Y-axis: True Positive Rate (Sensitivity)
- Three colored curves (LR, RF, GB) + diagonal reference line (random = AUC 0.500)
- All three curves expected to be very close to the diagonal (AUC ~0.520)

## Key Insights

- The three curves are nearly identical and nearly on the diagonal — visual confirmation of the weak predictive content
- Random Forest curve is marginally above the others (AUC = 0.5200, best of the three)
- The chart makes it visually clear that the model barely outperforms random classification
- Compare to Gu, Kelly & Xiu (2020) who achieve AUC ~0.57–0.58 with a large factor zoo — the gap shows how much better return-adapted features perform

## Key Concepts

- [[Machine Learning in Asset Pricing]] — AUC-ROC is the standard evaluation metric
- [[Reddit as Financial Signal]] — near-diagonal ROC confirms Reddit signals have limited alpha
